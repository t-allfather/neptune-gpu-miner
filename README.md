Neptune gpu taxminer

```console
./neptune-gpu-taxminer --node 45.149.206.49:3333 --proto phoebe --wallet $WALLET --worker $WORKER_NAME

# The --safe-power flag allows the miner to shut down gradually when no job is available,
# helping to avoid sudden power spikes.

./neptune-gpu-taxminer --node 45.149.206.49:3333 --proto phoebe --wallet $WALLET --worker $WORKER_NAME --safe-power

```
